{
  "name": "discohook-link",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "deploy-preview": "wrangler deploy --env=preview",
    "dev": "wrangler dev --port=8790"
  },
  "devDependencies": {
    "typescript": "^5.0.4",
    "wrangler": "^3.0.0"
  }
}
